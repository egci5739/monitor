<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "config/mybatis-3-mapper.dtd">
<mapper namespace="com.dyw.monitor.dao.EventLogDao">
    <!--更新进程id-->
    <insert id="addEventLog" parameterType="com.dyw.monitor.entity.EventLogEntity">
        insert into EventLogTable (eventLogContent) values (#{eventLogContent});
    </insert>
</mapper>